import { MyRequestCiDetails } from './my-request-ci-details';

describe('MyRequestCiDetails', () => {
  it('should create an instance', () => {
    expect(new MyRequestCiDetails()).toBeTruthy();
  });
});
